// TODO remove
pluginManagement {
    repositories {
        maven { url = 'https://repo.spring.io/snapshot' }
        gradlePluginPortal()
    }
}

rootProject.name = "demo-matchmaking"

include "playerprofile"